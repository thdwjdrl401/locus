# Locus M-MQTT 측정용 브로커 설정 (eclipse-mosquitto:2.0).
listener 1883
# 측정 LAN — 익명 허용(M0 HTTP 엔드포인트와 같은 정책, 인증·TLS는 M4). 공개는 nginx/인증과 함께.
allow_anonymous true
# 영속화 끔 — 측정 중 디스크 I/O 0. 버퍼는 파생 상태(ADR 0007 §버퍼 내 PII 최소화와 결).
persistence false
