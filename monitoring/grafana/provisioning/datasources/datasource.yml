# Grafana가 시작 시 Prometheus 데이터소스를 자동 등록(turnkey).
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # 고정 uid — provision된 대시보드가 참조.
    access: proxy
    url: http://prometheus:9090
    isDefault: true
