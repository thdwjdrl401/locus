# 보안 정책

이 저장소는 **공개(public)** 를 전제로 한다. 또한 도메인 특성상 **미성년자 위치 = 민감 개인정보**를 다루므로, 보안은 부가 기능이 아니라 설계의 1급 시민이다(계획서 §3·M6).

## 1. 비밀(secret)은 저장소에 들어가지 않는다
- DB 비밀번호·토큰 서명키·API 키 등 실제 비밀은 **절대 커밋하지 않는다.**
- 설정의 자격증명은 환경변수로 외부화한다. 예: `application.yml`의 `${DB_PASSWORD:...}`.
- 커밋된 기본값(`locuspw` 등)은 **로컬 docker-compose 전용 throwaway**이며 어떤 실제 환경에서도 쓰지 않는다.
- `.gitignore`가 `.env*`, `*.pem`, `*.key`, `*.jks`, `application-secret*.yml`, `application-local.yml` 등을 차단한다. (단 템플릿 `.env.example`은 예외로 커밋한다.)
- **`.env.example`** 은 필요한 env 키를 문서화하는 템플릿(placeholder 값)이다. 실제 값은 `cp .env.example .env`로 복사해 쓰며 `.env`는 커밋되지 않는다.
- CI(`.github/workflows/ci.yml`)가 **gitleaks**로 비밀 유출을 스캔한다.

## 2. 민감 데이터 취급 (M6에서 구현)
- **최소 수집**: `permission=DENIED` 또는 `sharingEnabled=false`면 위치를 수집·저장하지 않는다.
- **전송·저장 암호화**: 위치 등 민감 컬럼은 암호화(AES-GCM)한다.
- **보관 TTL·자동 파기**: 보관 기한 경과 데이터는 자동 파기한다.
- **평문 잔존 금지**: 복호화 데이터가 로그·힙 덤프에 평문으로 남지 않게 한다. 로그에 위치 평문을 출력하지 않는다(로그 마스킹).
- **동의 상태 기록**: 공유 동의(consent) 상태를 기록·조회 가능하게 한다.

> 국내 미성년자 위치정보는 위치정보보호법·개인정보보호법상 무겁게 다뤄진다(만 14세 미만 더 엄격). 본 저장소는 학습 목적이며, 실제 서비스화 시 구체 조항은 별도 확인이 필요하다.

## 3. 인증 (M4에서 구현)
- 디바이스: 장수명·폐기가능 토큰. 사람(교사): 단명 JWT + refresh.
- 즉시 폐기는 Redis 블랙리스트로 보장(분실·공유 해제 시). 상세는 M4 설계 시 결정(`docs/ROADMAP.md` 보류 결정).

## 4. 취약점 보고
이슈로 제보하거나 저장소 소유자에게 직접 연락. 민감한 내용은 공개 이슈 대신 비공개로.
